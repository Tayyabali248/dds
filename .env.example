# --- Web app / DB config ---
# Every machine running fillDDS.js needs MONGODB_URI too - that's how the
# authorization check (registered + enabled + fee cleared) is enforced.
MONGODB_URI=
SESSION_SECRET=
PORT=3000
